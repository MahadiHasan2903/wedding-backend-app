export declare enum PurchasePackageCategory {
    MONTHLY = "monthly",
    YEARLY = "yearly",
    LIFETIME = "life_time"
}
export declare enum PurchaseStatus {
    PENDING = "pending",
    SUCCEEDED = "succeeded",
    FAILED = "failed"
}
export declare enum PaymentStatus {
    PENDING = "pending",
    PAID = "paid",
    FAILED = "failed"
}
